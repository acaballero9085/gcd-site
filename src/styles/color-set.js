const COLOR_SET = new Map();

COLOR_SET.set(1, { backgroundColor: 'pea-green', fontColor: 'off-white' });
COLOR_SET.set(2, { backgroundColor: 'lulworth-blue', fontColor: 'off-white' });

export default COLOR_SET;
